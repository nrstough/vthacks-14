1. **Critical — The detailed API bypasses the monkeypatches the plan claims to preserve.** Step 4 calls `generate_with_fallback_detailed`, while `test_chat.py:543` and `:574` patch `app.chat.generate_with_fallback`. Keeping the old name exported does not intercept the new call; those tests will attempt real transport with their dummy keys. Update both patches and their return tuples, and correct the “breaks nothing” claim.

2. **Critical — Preset changes do not invalidate pending chat responses.** Clearing `suggestions` in a generation-dependent effect only removes suggestions already received. A request started before `adopt()` can finish afterward and repopulate stale controls because preset changes preserve the mounted panel. Capture the generation when sending and reject stale responses, or abort and correctly reset pending state on generation changes. Test a deferred response arriving after a preset switch.

3. **Critical — The proposed amount regex accepts Unicode digits.** Python’s `\d` is Unicode-aware: the exact pattern accepts `٥٠`, `５０`, and `12.٣٤`, and `int()` converts them successfully. Replace every `\d` with `[0-9]` or compile with `re.ASCII`; otherwise the promised rejection tests fail.

4. **Suggestion — The frontend interaction tests lack an executable testing strategy.** The current runner strips TypeScript and tests plain modules; it has no DOM or React interaction harness. Step 12’s assertions about controls, approval, and `adopt()` cannot be established by helper tests or a bundle grep. Specify a component/browser harness with mocked chat responses, or extract lifecycle logic into a tested module and separately verify the actual component wiring.

5. **Suggestion — The prompt retains a rule that conflicts with suggesting user-provided amounts.** `prompt.py` currently says “Use only figures that appear in the context below,” meaning the rendered solve. Step 5 changes the control instructions but does not explicitly exempt amounts supplied in conversation. A requested new balance absent from the solve would therefore be both permitted and forbidden. Explicitly allow copying user-stated amounts into suggestion markers while retaining the prohibition on computing hypothetical results.
